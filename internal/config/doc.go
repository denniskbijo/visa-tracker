// Package config provides environment-variable-based configuration
// for the visa-tracker server. All settings have sensible defaults
// and can be overridden via env vars (PORT, DB_PATH, SPONSOR_CSV_URL, etc.).
package config
